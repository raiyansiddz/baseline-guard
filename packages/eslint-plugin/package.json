{
  "name": "eslint-plugin-baseline-guard",
  "version": "1.0.1",
  "description": "ESLint plugin to warn on non-Baseline features",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "author": "Raiyan Siddique <raiyansiddique1801@gmail.com>",
  "license": "MIT",
  "repository": { "type": "git", "url": "https://github.com/raiyanz/baseline-guard.git" },
  "publishConfig": { "access": "public" },
  "scripts": {
    "build": "tsc -p ./",
    "clean": "rimraf dist",
    "test": "tsc -p ./"
  },
  "peerDependencies": {
    "eslint": ">=8.0.0"
  },
  "dependencies": {
    "web-features": "^2.0.0",
    "baseline-guard-core": "^1.0.1"
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "rimraf": "^5.0.0"
  }
}